#pragma once
#include "core.h"

#include <compare>

#include "ankerl/unordered_dense.h"

namespace nid {
using EntityId = usize;
using ComponentId = usize;
using ArchetypeId = usize;
} // namespace nid
